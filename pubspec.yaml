name: w_zentyar_app
description: App para zentyar
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ^3.35.0

dependencies:
  # State Management (Very Good)
  bloc: ^9.0.1
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # Speech Recognition - Whisper on-device
  sherpa_onnx: ^1.12.23

  # Audio Recording
  record: ^6.1.2

  # HTTP Client (obligatorio según DEVELOPMENT_RULES)
  dio: ^5.9.0

  # Archive extraction (tar.bz2)
  archive: ^4.0.7

  # Storage paths (flutter.dev official)
  path_provider: ^2.1.5

  # Permissions
  permission_handler: ^12.0.1
  audioplayers: ^6.5.1

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0

flutter:
  generate: true
  uses-material-design: true
